# 백엔드(API) 컨테이너 전용 의존성.
#
# 임베딩 모델·FAISS 는 AI 컨테이너에만 둔다. 여기에 섞으면 백엔드
# 이미지가 200MB 에서 4GB 로 불어나고 빌드도 10 배 느려진다.

fastapi
uvicorn[standard]
pydantic-settings
python-multipart
pypdf

# AI 컨테이너 호출용 HTTP 클라이언트(스트리밍 지원)
httpx

# 이미지 OCR(easyocr·pillow·numpy)은 여기 두지 않는다.
# easyocr 이 torch 를 끌고 와 이 이미지가 200MB 에서 3GB 로 불어난다.
# torch 가 이미 있는 AI 컨테이너에서 처리하고(POST /ocr) 결과 텍스트만 받아 온다.
#   요청: src/backend/services/ai_client.py  ->  처리: src/ai/main.py
